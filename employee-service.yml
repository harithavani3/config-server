spring:
 h2:
    console:
      enabled: true
      path: /h2-console
 jpa:
    hibernate:
      ddl-auto: update
 application:
  name: employee-service
  
 datasource:
  url: jdbc:h2:mem:testdb
  username: sa
  password:
  
      
